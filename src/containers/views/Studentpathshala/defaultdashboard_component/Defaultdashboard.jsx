import  React, { useState , useRef, useEffect } from 'react';
import './Defaultdashboard.css';

const Defaultdashboard = () => {
    useEffect(() => {
    },[]);
    return (
        <div className="row_full">
            this is the default dashboard area..
        </div>  
    );
}

export default Defaultdashboard;